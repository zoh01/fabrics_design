import 'package:flutter/material.dart';

class DetailsDotIndicator extends StatelessWidget {
  const DetailsDotIndicator({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
